import { Box, styled } from "@mui/material";

export const ClanOption = styled("li")({
  margin: 0,
  padding: 0,
});

export const ClanInput = styled(Box)({
  alignItems: "center",
  display: "flex",
  gap: "0.3rem",
});

export const ClanDetailLineContainer = styled(Box)({
  display: "flex",
  gap: "0.6rem",
});

export const ClanEmblemContainer = styled(Box)({
  alignItems: "center",
  display: "flex",
});
